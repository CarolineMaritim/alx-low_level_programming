#include <stdio.h>
#include <ctype.h>
/**
 * main - pogram the prints alphabet in lowercase
 *
 * Return: 0 (Success)
 *
*/
int main(void)
{
char ch;
putchar(tolower(ch));
putchar(toupper(ch));
putchar('\n');
return (0);
}
