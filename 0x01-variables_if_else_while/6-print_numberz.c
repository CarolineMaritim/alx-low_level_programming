#include <stdio.h>
/**
 * main - pogram the prints alphabet in lowercase
 *
 * Return: 0 (Success)
 *
*/
int main(void)
{
int main;
for (num = 0; num < 10; num++)
putchar((num % 10) + '0');
putchar('\n');
return (0);
}
